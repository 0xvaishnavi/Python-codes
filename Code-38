Python program  to find whether the given number is palindrome or not without the built-in rev() function.

n=int(input("Enter a number:- "))
org=n
reverse=0
while n>0:
    digit=n%10
    reverse= reverse*10+digit
    n=n//10
    
if org==reverse:
    print(f"{org} is palindrome.")
else:
    print(f"{org} is not a palindrome.")
