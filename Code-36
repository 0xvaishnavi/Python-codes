Python program to find a particular element out of the numbers entered by the user. As soon as the user enters a ‘-999’, stop taking in any further input from user and display whether the search element is present in the  given numbers.  
numbers = []
while True:
    num = float(input("Enter a number (enter -999 to stop): "))
    if num == -999:
        break
    numbers.append(num)

search = float(input("Enter the number to search: "))
print("Present" if search in numbers else "Not Present")
